import { StyleSheet } from 'react-native';

const olaStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        width: '100%',
        height: '100%',
        padding: 20,
        backgroundColor: 'aliceblue',
        borderColor: '#fff'
    },
    h1: {
        fontSize: 35,
        textAlign: 'center',
        marginBottom: 20
    },
    p: {
        fontSize: 15,
        fontWeight: 'bold',
        textAlign: 'center'
    }
});

export default olaStyles;